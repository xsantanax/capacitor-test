import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.politik.news',
  appName: 'capacitor-test',
  webDir: 'out',
  bundledWebRuntime: false
}

export default config
